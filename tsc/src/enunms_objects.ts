// Enums
enum Roles {
  User = 'USER',
  Admin = 'ADMIN',
  SuperAdmin = 'SUPERADMIN',
}

console.log(Roles.User)

// Objects
const roles = {
  User: 0,
  Admin: 1,
  SuperAdmin: 2,
}

console.log(roles.User)